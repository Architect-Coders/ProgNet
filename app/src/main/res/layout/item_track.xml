<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp">

    <TextView
        android:id="@+id/trackTitleTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Title: Title of the song"
        android:layout_marginBottom="8dp"
        />

    <TextView
        android:id="@+id/trackPositionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Position: Position of the song"
        android:layout_marginBottom="8dp"
        />

    <TextView
        android:id="@+id/trackDurationTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Duration: duration of the song"
        />


</LinearLayout>